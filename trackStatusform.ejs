<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Track Complaint Status</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
      padding: 40px 20px;
      margin: 0;
    }
    .container {
      width: 100%;
      max-width: 700px;
      background: #fff;
      padding: 30px 40px;
      border-radius: 20px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
      text-align: center;
    }
    h1 {
      color: #333;
      margin-bottom: 25px;
      font-weight: 600;
    }
    .search-form {
      display: flex;
      gap: 10px;
      margin-bottom: 30px;
    }
    .search-form input {
      flex-grow: 1;
      padding: 12px 15px;
      border: 2px solid #ddd;
      border-radius: 10px;
      font-size: 16px;
      transition: border-color 0.3s;
    }
    .search-form input:focus {
      outline: none;
      border-color: #667eea;
    }
    .search-form button {
      padding: 12px 25px;
      border: none;
      border-radius: 10px;
      background: #667eea;
      color: #fff;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s;
    }
    .search-form button:hover {
      background: #5a67d8;
    }
    .message {
      font-size: 18px;
      font-weight: 500;
      padding: 15px;
      border-radius: 10px;
      background-color: #fff3cd;
      color: #856404;
      border: 1px solid #ffeeba;
    }
    .complaint-details {
      margin-top: 20px;
      text-align: left;
      border: 1px solid #e9ecef;
      padding: 25px;
      border-radius: 15px;
      background-color: #fdfdfd;
    }
    .complaint-details h2 {
      margin-top: 0;
      color: #333;
      border-bottom: 2px solid #667eea;
      padding-bottom: 10px;
      margin-bottom: 20px;
      font-size: 22px;
    }
    .complaint-details p {
      margin: 12px 0;
      font-size: 16px;
      line-height: 1.6;
    }
    .complaint-details strong {
      color: #495057;
    }
    .status-badge {
      padding: 5px 12px;
      border-radius: 15px;
      color: #fff;
      font-weight: 600;
      font-size: 14px;
    }
    .back-link {
      display: inline-block;
      margin-top: 25px;
      color: #667eea;
      text-decoration: none;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Track Complaint Status</h1>
    <form action="/trackStatus" method="POST" class="search-form">
      <input type="text" name="caseId" placeholder="Enter your Case ID..." required>
      <button type="submit">Track</button>
    </form>

    <% if (message) { %>
      <div class="message"><%= message %></div>
    <% } %>

    <% if (complaint) { %>
      <div class="complaint-details">
        <h2>Complaint Details</h2>
        <p><strong>Case ID:</strong> <%= complaint.case_id %></p>
        <p><strong>Subject:</strong> <%= complaint.subject %></p>
        <p><strong>Description:</strong> <%= complaint.description %></p>
        <p><strong>Category:</strong> <%= complaint.category || 'N/A' %></p>
        <p><strong>Location:</strong> <%= complaint.location %></p>
        <p><strong>Submitted On:</strong> <%= new Date(complaint.created_at).toLocaleString() %></p>
        <p>
          <strong>Status:</strong> 
          <span class="status-badge" style="background-color: <%= getStatusColor(complaint.status) %>;">
            <%= complaint.status %>
          </span>
        </p>
      </div>
    <% } %>

    <a href="/" class="back-link">← Back to Home</a>
  </div>
</body>
</html>