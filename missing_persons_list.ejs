<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Missing Person Reports</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
<style>
  body {
    font-family: 'Poppins', sans-serif;
    margin: 0;
    padding: 20px;
    background: #141e30;
    color: #fff;
  }
  h1 {
    text-align: center;
    margin-bottom: 30px;
    color: #ffd700;
  }
  .container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 20px;
    max-width: 1200px;
    margin: 0 auto;
  }
  .card {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    padding: 20px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.3);
    backdrop-filter: blur(5px);
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .card img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-radius: 12px;
  }
  .card h2 {
    color: #ffd700;
    margin: 10px 0 5px;
  }
  .card p {
    margin: 0;
    line-height: 1.5;
    color: #eee;
  }
  .card .details {
    font-size: 14px;
    color: #ccc;
  }
  .card .status {
    align-self: flex-start;
    padding: 5px 10px;
    border-radius: 20px;
    font-weight: 600;
    font-size: 12px;
    text-transform: uppercase;
    background-color: #e91e63;
    color: white;
  }
  .no-reports {
    text-align: center;
    grid-column: 1 / -1;
    color: #ffcccc;
  }
</style>
</head>
<body>
  <h1>Missing Person Reports</h1>
  <div class="container">
    <% if (missing_persons && missing_persons.length > 0) { %>
      <% missing_persons.forEach(person => { %>
        <div class="card">
          <img src="<%= getMediaPath(person.media_path) %>" alt="Photo of <%= person.name %>">
          <h2><%= person.name %></h2>
          <p class="details"><strong>Age:</strong> <%= person.age %>, <strong>Gender:</strong> <%= person.gender %></p>
          <p class="details"><strong>Last Seen:</strong> <%= new Date(person.last_seen_date).toLocaleDateString() %> at <%= person.last_seen_location %></p>
          <p><strong>Description:</strong> <%= person.description %></p>
          <p class="details"><strong>Case ID:</strong> <%= person.case_id %></p>
          <span class="status"><%= person.status %></span>
        </div>
      <% }) %>
    <% } else { %>
      <p class="no-reports">No missing person reports found.</p>
    <% } %>
  </div>
</body>
</html>